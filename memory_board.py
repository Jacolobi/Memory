from tkinter import *

class board:
    def __init__(self, parent):
        self.parent = parent #vad är detta?:(
        self.title("Cow Moomory")
        background = Frame(memory, height(), widht(), image=("gräs"))
        
    def create_buttons(self):
        self.buttons = (Tkinter.Button(memory, image("sarah ansikte"), command=()))
        
    
    def create_progress_bar(self):
        label_time = Label(memory, text = "Time:")
        label_moves = Label(memory, text = "Mooves:")
        label_pairs = Label(memory, text = "Moos")
        
    def adapt_size
    
back_to_menu = Tkinter.Button(memory, text = ("Quit and return to menu"), command = menu).place(relx = (), rely = ()) 
"""Sätt någonstans nära progress bar"""
back_to_menu.pack("memory??")
                                 
memory = Tk()

memory.mainloop()